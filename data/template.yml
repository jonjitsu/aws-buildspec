---
version: 0.1

environment_variables:
  plaintext:
    key: "value"
phases:
  install:
    commands:
      - echo install
  pre_build:
    commands:
      - echo pre-build
  build:
    commands:
      - echo build
  post_build:
    commands:
      - echo post-build
artifacts:
  files:
    - location
  discard-paths: yes
  base-directory: location
